export interface SkillInterface {
  web: {
    skill: string;
    comp: number;
  };
  programming: {
    skill: string;
    comp: number;
  };
  mobile: {
    skill: string;
    comp: number;
  };
  database: {
    skill: string;
    comp: number;
  };
}
